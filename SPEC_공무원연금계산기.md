# 공무원연금 계산기 — 개발 스펙 (Claude Code 인계용)

이 문서는 구현 지시서입니다. 법령 파라미터의 근거·출처·검증 이력은 같은 폴더의
`공무원연금_계산기_파라미터_레퍼런스.md`에 있습니다. **수치를 코드에 넣기 전 반드시 그 문서를 읽으세요.**

---

## 0. 이 프로젝트에서 가장 중요한 것

연금 계산기는 **조용히 틀리는 것이 가장 나쁩니다.** 사용자는 결과가 틀렸는지 알 방법이 없고,
퇴직 시점 의사결정에 쓰입니다. 따라서:

1. **계산 엔진은 순수 함수로 분리하고 단위 테스트를 먼저 작성합니다.** UI보다 엔진이 우선입니다.
2. **법령 표는 하드코딩하지 말고 데이터 파일에서 로드합니다.** 표가 개정되면 데이터만 교체합니다.
3. **가정값은 전부 화면에 노출합니다.** 보수인상률 등 미래 가정은 숨기지 말고 사용자가 조정할 수 있게 합니다.
4. **불확실한 항목은 계산하지 말고 명시적으로 "확인 필요"로 표시합니다.** (아래 §7 미해결 항목)

---

## 0-1. 레퍼런스 구현체

`reference_engine.py`가 같은 폴더에 있습니다. **TypeScript 엔진을 짠 뒤 동일 입력으로 출력을 대조하세요.**
프로덕션 코드가 아니라 대조용 기준 구현입니다. 실행하면 회귀 테스트가 돌아갑니다.

```
python3 reference_engine.py
```

포팅할 때 특히 주의할 부분: 재직기간 3분할(`_배분`), 이행률 열 인덱싱(`이행률조회`),
평균기준소득월액 환산(`q ** (k/12)` 누적), 역전방지 상한(`min(개정, 종전규정)`).

---

## 1. 스택

- **Vite + React + TypeScript** (SPA, 정적 배포)
- 테스트: **Vitest**
- 상태: 로컬 상태로 충분. 전역 상태 라이브러리 불필요
- 스타일: Tailwind
- **서버 없음.** 모든 계산은 브라우저에서 수행하고 입력값을 외부로 전송하지 않습니다.
  개인 소득 정보를 다루므로 이건 요구사항입니다. 화면에도 명시하세요.

---

## 2. 데이터 파일

같은 폴더의 CSV를 `src/data/*.json`으로 변환해 번들에 포함합니다. 변환 스크립트를 `scripts/`에 두고
원본 CSV도 리포에 커밋해 추적 가능하게 하세요.

| 원본 | 내용 |
|---|---|
| `이행률표_시행령_부칙_제10조제10항.csv` | 이행률 33행 × 34열 삼각행렬 (유효 셀 594개) |
| `보수인상률_물가변동률_시계열.csv` | 2009~2026 보수인상률 / 물가변동률 / 연금인상률 |
| `전체공무원_기준소득월액_평균액.csv` | 2011~2026 인사혁신처 고시액 (A값·상한 산정용) |
| `참고_전체재직공무원_1인당평균기준소득월액.csv` | **계산에 쓰지 말 것.** 모집단이 다른 통계. 교차검증용 |

코드에 직접 넣어야 하는 표(레퍼런스 문서에서 옮길 것):
- 연도별 연금지급률 2016~2035 (§2)
- 소득재분배 적용비율 15개 구간 (§3)
- 기여율 단계 (§5), 재직기간 상한 단계 (§6), 지급개시연령 (§7), 조기감액률 (§8)
- 퇴직수당 지급비율 (§11)
- 전체 공무원 기준소득월액 평균액 연도별 (§9) — **일부 연도만 확보됨, §7 참조**

### 데이터 로드 시 자동 검증 (필수)

`src/data/validate.ts`에 다음 불변식을 두고 앱 부팅 시 검증, 실패하면 개발 모드에서 throw:

- 이행률표: 행 k의 유효 셀 수 = 35 − k (삼각구조)
- 이행률표: 각 행 첫 값이 행 번호에 대해 단조증가
- 이행률표: `신규자` 열이 단조증가하며 범위가 77.25 ~ 103.44
- 소득재분배표: 구간 하한 r에 대해 적용비율 ≈ (1 + 0.5(r−1)) / r
- 지급률표: 2016년 1.878 → 2035년 1.700, 연도별 인하폭이 0.022 / 0.01 / 0.004
- 인상률 시계열: 연금인상률(t) == 물가변동률(t−1) (2021~2026 구간)
- 기준소득월액 평균액: 2011~2026 전 연도 존재, 2021년만 전년 대비 감소(−0.74%)

**기준소득월액 평균액에 단조증가를 가정하지 마세요.** 2021년에 코로나19 여파로
5,390,000 → 5,350,000으로 감소합니다. 2020년과 2022년이 둘 다 5,390,000원인 것도 이 때문입니다.
단조성 기반 보간·검증 로직을 넣으면 이 지점에서 깨집니다.

이 검증은 이미 수동으로 통과시킨 것들입니다. 회귀 방지용으로 코드에 남기세요.

---

## 3. 계산 엔진 스펙

### 3.1 최상위 산식

```
월 퇴직연금액 = 1기간액 + 2기간액 + 3기간액
```

재직기간을 임용일·퇴직일 기준으로 세 구간으로 자른 뒤 각각 계산해 합산합니다.
**구간 경계는 2009-12-31 / 2015-12-31입니다.**

### 3.2 1기간 (2009-12-31 이전)

```
재직연수 ≥ 20년: 평균보수월액 × 0.50 + 평균보수월액 × (재직연수 − 20) × 0.02
재직연수 <  20년: 평균보수월액 × 재직연수 × 0.025
```

`평균보수월액` = 2007~2009년 보수월액을 보수인상률로 2009년말 현재가치 환산 → 합산 → 재직월수로 나눔
→ 급여사유 발생시점으로 재환산.

### 3.3 2기간 (2010-01-01 ~ 2015-12-31)

```
평균기준소득월액 × 이행률 × 재직기간(년) × 0.019
```

### 3.4 3기간 (2016-01-01 이후)

```
소득재분배분 = 소득재분배_평균기준소득월액 × 이행률 × min(재직기간, 30년) × 0.01
개인소득분   = Σ_연도 [ 평균기준소득월액 × 이행률 × 해당연도_재직기간 × (연도별지급률 − 0.01) ]
3기간액 = 소득재분배분 + 개인소득분
```

**지급률은 재직 연도별로 각각 적용합니다.** `재직연수 × 퇴직연도지급률`로 계산하면 틀립니다.
2016년 이후 각 연도를 순회하며 그 해의 지급률을 적용해 누적하세요.

30년 초과분은 소득재분배 없이 전액 소득비례입니다.

### 3.5 소득재분배

```
A = 퇴직 전 3년간 공무원 전체 기준소득월액 평균액 (물가변동률로 환산 후 3으로 나눔)
B = 본인 전 기간 평균기준소득월액
ratio = floor(B / A, 소수 둘째자리)   // 공단 사례상 절사
적용비율 = 구간표 조회(ratio)
C = B × 적용비율
```

구간 경계에서 계단식으로 떨어집니다. 소득이 조금 올라 연금이 줄어드는 구간이 실제로 존재하므로,
UI에서 사용자가 경계 근처면 그 사실을 알려주세요.

### 3.6 역전 방지 상한 (빠뜨리기 쉬움)

2016년 이후 재직기간 급여액을 산정할 때, **소득재분배 적용 산식으로 계산한 금액이
종전 규정 산식보다 크면 종전 규정 금액으로 지급합니다.** 두 값을 모두 계산해 `Math.min`을 취하세요.
저소득 구간(적용비율 105.56~300%)에서 반드시 걸립니다.

### 3.7 현재가치 환산

```
환산액 = 원금액 × Π (1 + 해당 연도 공무원보수인상률)
```

적용기간별로 차례로 곱합니다. 재직기간 합산자의 종전 퇴직~재임용 사이 구간만 보수인상률 대신
**물가변동률**을 씁니다. A값 환산도 물가변동률입니다. 지표를 섞지 마세요 (레퍼런스 문서 §10 표 참조).

**2016~2020년 연금 인상률 0%는 한시 동결이며 환산·A값에는 적용되지 않습니다.**
이 구간에는 실제 물가변동률(1.0 / 1.9 / 1.5 / 0.4 / 0.5)을 씁니다.

### 3.8 후처리

1. 재직기간 상한 적용 (2015년말 재직기간에 따라 33~36년)
2. 조기퇴직 선택 시 미달연수별 감액 (95/90/85/80/75%)
3. 지급개시연령 판정 (퇴직연도 + 임용시기 경과규정)
4. 물가연동 조정은 **수급 개시 이후** 매년 적용 — 최초 연금액 계산과 분리하세요

---

## 4. 화면

단일 페이지, 3영역.

**입력**
- 임용일, 퇴직예정일
- 소득 입력 방식 선택: ① 기준소득월액 직접 입력(권장, 연금복지포털에서 확인 가능) ② 직급·호봉 기반 추정
- 조기퇴직 여부
- 가정값 (접이식, 기본값 제시): 미래 보수인상률, 미래 물가변동률

**결과**
- 최초 월 연금액 (개시연령 표기)
- 3구간별 기여 내역 — 어느 구간이 얼마를 만들었는지 반드시 분해해서 보여주세요
- 적용된 이행률·소득재분배 적용비율·연도별 지급률을 그대로 노출
- 퇴직수당 (별도 항목)
- 역전 방지 상한이 걸렸으면 그 사실 표시

**고지**
- "추정치이며 법적 효력이 없습니다. 정확한 연금액은 공무원연금공단(1588-4321)에서 확인하세요."
- "입력값은 브라우저를 벗어나지 않습니다."
- 근거 법령 조문 표기

---

## 5. 테스트 (엔진 구현 전에 작성)

### 5.1 소득재분배 — 공식 예시 (사학연금공단 개정 설명자료)

```
A = 4,380,000
① B = 6,000,000 → ratio 1.36 → 적용비율 88.46% → C = 5,307,600
② B = 1,500,000 → ratio 0.342 → 적용비율 216.67% → C = 3,250,050
```

### 5.2 구조 불변식
§2의 검증 항목 전부를 테스트로 옮깁니다.

### 5.3 통합 테스트 — 실제 케이스

`reference_engine.py`의 `CASE` / `EXPECTED`. 3개 구간과 역전방지 상한을 모두 통과하는 케이스입니다.

```
입력: 1990년생, 2019.3 임용(교사), 2047.8 퇴직, 기준소득월액 4,300,000원(2026),
      군복무 2009.6~2011.3 산입
가정: 보수인상률 2.3% / 물가 2.1% / 호봉승급기여 1.5%

기대: 재직 30.33년 (1기간 0.58 / 2기간 1.25 / 3기간 28.50)
      이행률 100.84%  (종전 1월~1년 미만 열 × 이후 29년초과~30년이하 행)
      B/A 0.79 → 적용비율 121.43%
      3기간: 재분배 5,084,641 > 종전규정 4,522,268 → ★상한 적용, 4,522,268
      월 연금액 4,874,116원 / 개시 2055년 65세 / 무연금 8년
```

이 케이스가 잡아주는 것:
- 군복무 산입기간이 2009년·2010년에 걸쳐 **1기간과 2기간으로 쪼개지는** 처리
- 종전기간이 1년 미만일 때 이행률 열이 `신규자`가 아니라 `1월이상~1년미만`이 되는 처리
- 소득재분배가 유리하게(112.5%) 나왔는데 **역전방지 상한에 막히는** 처리
- 명예퇴직이라 지급개시연령 예외에 해당하지 않아 8년 무연금이 발생하는 처리

### 5.4 경계 케이스
- 2009-12-31 임용 vs 2010-01-01 임용 (1기간 유무)
- 2015-12-31 vs 2016-01-01 (소득재분배 유무)
- 재직 10년 미만 → 퇴직일시금 경로
- 재직 30년 초과 → 소득재분배 미적용 구간 분리
- 재직 36년 초과 → 상한 절단
- 소득재분배 구간 경계 (B/A = 1.09 vs 1.10) → 100% vs 95.45%
- 역전 방지 상한이 걸리는 저소득 케이스

### 5.5 퇴직일시금 산식
```
기준소득월액 × 재직연수 × [975/1000 + 65/10000 × (재직연수 − 5)]
```
산정액이 "기여금 + 민법 제379조 이자"보다 적으면 후자를 지급.

---

## 6. 구현 순서

1. 데이터 변환 + 검증 모듈 (§2)
2. 이행률·지급률·소득재분배 조회 함수 + 테스트
3. 현재가치 환산 함수 + 테스트
4. 2기간 계산 (환산 + 이행률, 소득재분배 없음) — 가장 단순하므로 여기서 골격을 잡습니다
5. 3기간 계산 (소득재분배 + 연도별 지급률 + 역전 방지)
6. 1기간 계산
7. 후처리 (상한·감액·개시연령)
8. UI
9. 퇴직수당·퇴직일시금

각 단계마다 `reference_engine.py`와 출력을 대조하며 진행하세요.

---

## 7. 미해결 항목 — 임의로 채우지 말 것

아래는 확정되지 않았습니다. **추정해서 계산하지 말고, 해당 입력이 필요한 경우 사용자에게
"이 항목은 확인이 필요합니다"로 안내하거나 기능을 비활성화하세요.**

1. **2009·2010년 기준소득월액 평균액** — 고시 제도가 2011년 시작이라 그 이전 값이 없습니다.
   시행령 부칙 제10조제6항의 1기간 환산식에 "2008년도·2009년도 과세소득에 대한 평균액"이 필요하므로,
   **2009년 이전 임용자를 지원할 때만** 문제가 됩니다. MVP 범위(2010년 이후 임용자)에서는 무관합니다.
2. **보수인상률 고시 원문 대조** — 현재 값은 예산상 확정 인상률. 2024년(2.5%)만 공단 안내와 일치 확인됨.
   법령상 근거는 시행령 제5조제5항 고시.
3. **2019년 보수인상률** — 1.8%(다수 출처) vs 1.5%(소수) 혼재. 1.8% 채택했으나 미확정.
4. **2010~2019년 소비자물가변동률** — KOSIS 원출처 대조 미실시.
5. **군복무 산입기간의 소득 취급 (2가지)** — `reference_engine.py`에 분기로 노출해 두었습니다.
   - `일기간소득인정`: 2009년 이전에 걸친 군복무분의 1기간 소득을 인정할지.
     1기간은 원래 2007~2009년 본인 **보수월액** 기준인데 복무 중이라 보수월액이 없습니다.
     기준 케이스에서 **월 20만원** 차이가 납니다.
   - `군복무_평균산입`: 군복무 기간을 평균기준소득월액 산정 분모에 넣을지.
     기준 케이스에서 **월 7만원** 차이가 납니다.

   둘 다 법령 문언만으로 확정되지 않습니다. UI에서 "공단 확인 필요"로 표시하고
   두 값의 범위를 함께 보여주는 것을 권합니다.

6. **명예퇴직 처리** — 법 제43조제1항의 2~4호(60세 미만 정년/계급정년/직제개폐)에
   해당하지 않으므로 지급개시연령 예외가 없습니다. 퇴직~개시 사이 **무연금 기간**을
   반드시 화면에 표시하세요. 조기퇴직연금은 미달연수 5년이 한도라 그 이상은 당길 수 없습니다.
   명예퇴직수당은 별도 규정(「국가공무원 명예퇴직수당 등 지급 규정」)이며 연금액에 반영되지 않습니다.

7. **평균보수월액(1기간) 산정** — 2007~2009년 개인 보수월액이 필요한데 사용자가 알기 어렵습니다.
   2009년 이전 임용자 지원 여부를 먼저 결정하세요. MVP에서는 2010년 이후 임용자만 지원하는 것을 권합니다.

---

## 8. 하지 말 것

- 이행률표 `7년 이상~8년 미만` 열의 꺾임을 보간으로 펴지 마세요. 법령 표 자체의 성질입니다.
- 소득재분배 구간표를 선형 보간하지 마세요. 계단 함수입니다.
- 역전 방지 상한을 생략하지 마세요.
- 지급률을 퇴직연도 값으로 일괄 적용하지 마세요.
- 서버로 입력값을 보내지 마세요.
- 결과를 단정적으로 표기하지 마세요. 항상 추정치임을 병기합니다.
- 기준소득월액 평균액 시계열에 단조증가를 가정하지 마세요 (2021년 감소).
- `참고_전체재직공무원_...csv`를 A값이나 상한 계산에 쓰지 마세요. 모집단이 다릅니다.
